mutation IntroductionMutation($title: String!, $content: String!) {
  updateIntroduction(input: { data: { title: $title, content: $content } }) {
    introduction {
      title
      content
    }
  }
}
